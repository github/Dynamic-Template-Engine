name: GitHub CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: TestHello
      id: testHello
      uses: ./
      with:
        repoName : anuraag016/TemplateEngineActionTest
        branchName : master
        templateType: HandleBars
        sourceType: CodePushed
        ClientType: Teams
    - name: PrintOutput
      run: echo ${{ github.event.repository }}
