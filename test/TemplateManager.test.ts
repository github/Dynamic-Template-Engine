import TemplateManager from '../src/TemplateManager';
jest.mock('../src/Utility/Utility');
describe('TemplateManager', () => {
  it('setups templates from local filesystem', () => {
    //TemplateManager.setupTemplateConfiguration('../src/TransformerConfig.json');
  });

  it('', () => {

  });
});